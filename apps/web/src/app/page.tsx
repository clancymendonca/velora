import { Card } from '@velora/ui';

export default function HomePage() {
  return (
    <main className="mx-auto max-w-2xl p-8">
      <Card title="Velora monorepo skeleton">
        Next.js web app is configured with Tailwind, shared packages, and backend scaffolding.
      </Card>
    </main>
  );
}
